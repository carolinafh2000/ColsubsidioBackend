server:
  port: 8080
  servlet:
    context-path: /
    session:
      timeout: 21600s
springdoc:
  swagger-ui:
    path: /swagger-ui.html
spring:
  datasource:
    driver-class-name: org.h2.Driver
    jdbcUrl: jdbc:h2:mem:pruebadb;MODE=MySQL;DB_CLOSE_DELAY=-1;DATABASE_TO_UPPER=FALSE;CASE_INSENSITIVE_IDENTIFIERS=TRUE
    username: userdb
    password: Pru3b4C0l5ub51d10
    hikari:
      poolName: pool
  h2:
    console:
      enabled: true
      path: /h2-console
  flyway:
    baselineOnMigrate: true
    validateOnMigrate: false
security:
  jwt:
    secret: secret
    expiration: 36000